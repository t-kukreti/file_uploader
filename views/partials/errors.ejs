<% if (errors && errors.length > 0) { %>
    <div class="errors">
        <% errors.forEach(error => { %>
            <p class="error" style="color: red;"><%= error.msg %></p>
        <% }) %>
    </div>
<% } %>